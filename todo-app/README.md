# Todo app

A minimal web server for the course project. The listening port is configured with `PORT`.

## Run

```bash
PORT=8000 python app.py
```

## Build

```bash
docker build -t todo-app:1.2 .
```
